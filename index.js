//Welcome to JavaScript code tutorial!
console.log('Hello World!');

//Dynamic typing - Allows to type in the console 
//The typeof() function returns type of variable

//, The comma acts as a multi-argument separator for the console.log() method
//Each argument is evaluated individually and appended to the log entry with a space in between
//Example: console.log('User object:', userObject);

//+ It concatenates the arguments into a single string before they are passed to console.log(). If any operand is a string, all operands are coerced into strings (string concatenation)
//All elements are treated as part of a single string expression, which can sometimes lead to unexpected results with complex types
//Example: console.log('User object: ' + userObject); (will output a flat string) 